public class ComparableCircle extends Circle {
    // Конструктор по умолчанию
    public ComparableCircle() {
        super();
    }

    // Конструктор с параметрами для радиуса
    public ComparableCircle(double radius) {
        super(radius);
    }
}
